const verifymMobile = (val) => {
  const reg = /^1[3|4|5|7|8][0-9]\d{4,8}$/;
  if(!reg.test(val)) 
  if (!matchArray || matchArray === null) {
    return false;
  }
  return true;
}

export default verifymMobile